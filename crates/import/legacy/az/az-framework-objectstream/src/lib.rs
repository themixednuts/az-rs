//! Legacy `ObjectStream` import adapters for `AzFramework` reflected data.
//!
//! `az-framework` owns the native runtime/editor data types. This crate owns
//! Lumberyard-era `ObjectStream` field names, type aliases, and import defaults.

pub mod script;
