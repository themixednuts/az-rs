[package]
license.workspace = true
repository.workspace = true
name = "az-engine-mount"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Fixture engine bundle crate taking its identity from an engine manifest"
publish = false

[lints]
workspace = true

[dependencies]
az-gem-contract = { workspace = true }
