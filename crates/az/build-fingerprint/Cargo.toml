[package]
license.workspace = true
repository.workspace = true
name = "az-build-fingerprint"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Build-script helper that derives a deterministic fingerprint of a crate's own build inputs"

[lints]
workspace = true

[dependencies]
blake3 = { workspace = true }
toml = { workspace = true }
