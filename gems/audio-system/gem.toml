[manifest]
kind = "gem"
schema = "azoth.gem/v3"

[gem]
id = "azoth.audio-system"
name = "AudioSystem"
version = "0.1.0"

[paths]
assets = "assets"
scripts = "scripts"

[[contributions]]
id = "package"
kind = "code"
package = "az-gem-audio-system"
roles = ["game", "p2p", "client", "unified", "project-host", "runtime-host"]
